"use client";

import React, { ReactElement } from "react";

import DashboardPage from "@/components/Pages/DashboardPage";

/**
 * Creates a React element for the Page component.
 *
 * @return {ReactElement} The React element representing the Page component.
 */
function Page(): ReactElement {
  return <DashboardPage />;
}

export default Page;
